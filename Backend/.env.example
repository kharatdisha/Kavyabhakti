## Copy this to .env and UNCOMMENT/FILL VALUES

# Database (MySQL - local or hosted like PlanetScale)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=rekha@83
DB_NAME=kavyabhakti_medical
DB_PORT=3306

# JWT (generate strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL(s) for CORS (comma-separated)
FRONTEND_URL=http://localhost:5500,https://your-site.netlify.app

# Optional
NODE_ENV=production
PORT=3000
